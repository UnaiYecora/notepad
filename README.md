# Notepad

Another serverless notepad PWA. Works offline, auto-saves locally (and only locally) and features saving multiple notes, a few themes, markdown viewer, and Picture-in-Picture mode (to have a little always-on-top notepad window).

*Use CTRL+S to quickly save a note.